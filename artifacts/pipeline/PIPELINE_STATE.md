# THUS Journal — Pipeline State

**Updated (local):** 2026-07-08
**Local HEAD:** `f01eb33` == `origin/main` — **in sync, DEPLOYED**.
**Deploy posture:** **DEPLOYED** — pushed `71283c3..f01eb33` (13 commits: G2 v0.4 loader/render/reset + MT5 dry-run harness merge `3f4a67d` + version bump `f01eb33`); Netlify published. Prod `thus999.com` serves index.html **byte-identical** to `f01eb33` (595,900 B, sha256 `4f8564da…`, HTTP 200). App version **v3.23.0** (single-source `APP_VERSION`). G2 v0.4 loader/render is **live default-off**; write gate NOT enabled. **No-auth default-off smoke PASS** (app mounts; flags null; 0 `create_trade_group_v1`; 0 `/rest/v1/trades` + 0 `/rest/v1/trade_groups` writes on load; grouping UI absent; no ⛓ badge). **Authenticated Positions/Journal/P&L visual smoke PASS (user browser, 2026-07-08)** → **v3.23.0 deploy verification COMPLETE.**

> This is a glance-able state file for autopilot. Autopilot **preserves adversarial
> review** — see [`AUTOPILOT_RULES.md`](./AUTOPILOT_RULES.md). It may inspect / summarize /
> run static checks / do low-risk local docs+UI work on already-reviewed scope, and MUST
> STOP for anything on the stop list (DB/SQL/RLS, Supabase/RPC writes, push/deploy, enabling
> write gates, MT5 writer, GUGU cognition, durable save/close/delete/merge/import, failed
> validation, architecture/product tradeoffs).

---

## Lanes

| Lane | Title | Status | Next action | Gate |
|---|---|---|---|---|
| **A** | Deploy batch | **DONE + VERIFIED** — deployed `f01eb33` to prod (**v3.23.0**), byte-identical (595,900 B, sha `4f8564da…`); no-auth default-off smoke PASS + **authenticated visual smoke PASS** (2026-07-08) | — (next deploy is a fresh user-gated batch) | push/deploy = user approval |
| **B** | G2 grouping | v0.4 loader/render **DEPLOYED + VERIFIED default-off** (`ba9e780`+`9a07fdc`, live at `f01eb33` / v3.23.0); no-auth + **authenticated** smoke PASS. v0.5 ungroup UI **design approved-deferred** (ChatGPT PASS → IMPLEMENT_AFTER_CURRENT_DEPLOY). DB safe (0 active / 0 grouped, 1 archived); flags default-off, write gate NOT enabled | Separately-gated: enable `tj_trade_group_write_v01` + keep a real group; or implement v0.5 ungroup | flag enable = user approval; keep group = user approval |
| **C** | Product/MT5 preview UX cleanup | **DONE** — MT5 Inbox preview (`85e5116`) + trade-open picker card csize (`3a0b258`); review-summary row optional/deferred | (optional) review-summary "Contract Size" row | scope review before code |
| **D** | MT5 auto draft import | Real staging writer **designed, not implementing** (gated). Offline **dry-run harness merged/deployed** (`85be5b2`+`8fb9a33` via `3f4a67d`; `ops/mt5_import/dry_run.py`) — no Supabase/MT5/network, tests PASS, deterministic | Design review before any real writer code; harness stays dry-run only | review + architecture gate; staging writer = user approval |
| **E** | GUGU bot | Capture-only / **cognition freeze** | Backlog: market-aware cadence, review_week, review_position, snooze, group-aware check-ins | cognition/autonomy = STOP |
| **F** | Merge/grouping boundary | **AUDITED** (`8c01f95`) — CLEAR_FOR_BATCH_DEPLOY_AS_IS; isMerged exclusion shipped (`b1f8e7d`); RPC-side guard **design reviewed + approved (ChatGPT PASS)** ([`../g2_grouping/g2_rpc_ismerged_hardening_design.md`](../g2_grouping/g2_rpc_ismerged_hardening_design.md)) | Apply RPC-side `raw->>'isMerged'` guard (function-body replace) post-v0.4-deploy, pre-write-gate — **run read-only precheck (expect 0) first** | design ✅ approved; SQL apply = user (SQL Editor, BEGIN/ROLLBACK) |
| **G** | RLS / security hardening | High-risk | Fresh **read-only** audit required first | RLS/schema = STOP |
| **H** | P2-5 image externalization | **CLOSED** (18/18 backfilled) | Backup retention decision later (holds sensitive base64) | delete backup = user approval |
| **I** | Mentor / GUGU notes | Backlog | — | — |

---

## Detail notes

- **A — Deploy batch. DONE (2026-07-08) — now `f01eb33` / v3.23.0.** Pushed `71283c3..f01eb33` (13 commits: G2 v0.4 loader/render/reset + MT5 dry-run harness merge `3f4a67d` + version bump `f01eb33`); Netlify published. Prod thus999.com serves index.html **byte-identical** to `f01eb33` (595,900 B, sha256 `4f8564da…`, HTTP 200). Version badge now **v3.23.0** via single-source `APP_VERSION`. **No-auth default-off smoke PASS** (app mounts; `tj_trade_group_ui_v01`/`tj_trade_group_write_v01`/`tj_trade_group_mock` all null; grouping UI absent; 0 `create_trade_group_v1`; 0 `/rest/v1/trades` + 0 `/rest/v1/trade_groups` writes on load; no ⛓ badge). **Authenticated Positions/Journal/P&L visual smoke PASS (user browser, 2026-07-08) → v3.23.0 deploy verification COMPLETE.** See [`../g2_grouping/g2_v04_mt5_harness_deploy_closeout.md`](../g2_grouping/g2_v04_mt5_harness_deploy_closeout.md). *(Prior: `71283c3`/v3.22.0 batch `f5290f7..b1f8e7d` + hotfix.)* A *future* deploy is again a fresh user-gated batch.
- **B — G2 grouping.** Applied migration `20260705_g2_trade_group_rpcs.sql`; SQL rollback smoke PASS; UI v0.3 create-only + `isMerged` candidate exclusion (`b1f8e7d`, ROADMAP #184) deployed default-off. **Live write-gate browser smoke DONE 2026-07-07 — PASS WITH ROLLBACK** ([`../g2_grouping/g2_write_gate_browser_smoke_closeout.md`](../g2_grouping/g2_write_gate_browser_smoke_closeout.md)): real UI create (group `f49056fa`, label `gold Long`) → one `create_trade_group_v1` call, no direct table writes, both children grouped, `raw` byte-identical (P/L invariant held live) → ungrouped. DB now safe: 0 active groups / 0 grouped trades; one **archived** group row remains by design. Flags cleared; grouping UI absent again; write gate NOT enabled. **G2 v0.4 group-aware loader/render — DEPLOYED DEFAULT-OFF 2026-07-08 (`ba9e780`+`9a07fdc`, live at `f01eb33` / v3.23.0; Codex PASS)** ([`../g2_grouping/g2_v04_loader_render_closeout.md`](../g2_grouping/g2_v04_loader_render_closeout.md)): `db.loadAll` now selects `raw,group_id` and returns a SEPARATE `id→group_id` map (`tradeGroupIds`); `trades[]` stays `raw`-only and `group_id` never touches `raw`/trade objects (`toTradeRow` unchanged → P/L invariant preserved). Grouped rows are suppressed from candidates before bucketing and shown with a distinct green ⛓ badge; no collapse/nesting/reorder. Stale-`tradeGroupIds` reset added on hydration/auth transitions + failure/catch paths (`9a07fdc`). Codex flagged one **pre-existing** whole-hydration async race (superseded `loadAll` can repopulate) as out-of-scope, not a blocker. Residual: RPC-side `raw->>'isMerged'` guard (pre-write-gate, separate schema review). **G2 v0.5 ungroup UI — DESIGN REVIEWED + APPROVED-DEFERRED (ChatGPT PASS → IMPLEMENT_AFTER_CURRENT_DEPLOY)** ([`../g2_grouping/g2_v05_ungroup_design_closeout.md`](../g2_grouping/g2_v05_ungroup_design_closeout.md)): write-gated Ungroup action in the trade **detail modal**, visible only when `groupUiV01On && groupWriteV01On && group_id present`; typed `UNGROUP` confirm; exactly one `ungroup_trade_group_v1({p_group_id})` RPC (no direct table writes); success clears `tradeGroupIds` for the returned `child_ids` (group_id-map fallback if `already_archived`); no `trade_groups` read, no collapse/nesting, no raw/P&L/reducer touch. **Not part of the current v0.4 batch** — implement only after v0.4 deploys + default-off smoke passes (then write-flag enable + a real kept group to smoke against). Also see [`../g2_grouping/g2_v03_ui_and_rpc_smoke_closeout.md`](../g2_grouping/g2_v03_ui_and_rpc_smoke_closeout.md), [`../g2_grouping/merge_grouping_boundary_audit.md`](../g2_grouping/merge_grouping_boundary_audit.md).
- **C — Product/MT5 preview UX cleanup.** **DONE** for the MT5 Inbox Mapping preview surface (`85e5116`, 2026-07-07): `_mt5PreviewLabel`/`_mt5ContractPair` now render family-level `Name — base · Current <cur> · Next <nxt> · csize <n>` with the `matches row (current|next)` suffix preserved; display-string helpers only, no product_id/registry/persistence change; esbuild EXIT 0, 16/16 unit. **DONE** for the trade-open product picker cards too (`3a0b258`, 2026-07-07): step-0 cards show a display-only ` · size <n>` fragment via `_csizeFrag(p)` (family-level; omitted when absent). `f.productId` still set from `s.id`; `buildTrade`/`toTradeRow`/`raw` unchanged; esbuild EXIT 0, 11/11 unit + 9/9 component smoke. **Deferred (optional, low priority):** the order review-summary "Contract Size" row was explicitly excluded — selection-time visibility is now solved, so this is polish, not a gap. The `productId`-driving picker was changed display-only (no selection/persistence change).
- **D — MT5 auto draft import.** Real staging-writer draft import touches durable/import paths → STOP for review (designed, not implementing). **Offline dry-run harness merged + deployed 2026-07-08** (`85be5b2`+`8fb9a33` via merge `3f4a67d`; [`../mt5_import/README.md`](../mt5_import/README.md), `ops/mt5_import/dry_run.py`, Codex PASS): fixture-driven, reuses `build_rows.py` pure mappers, adds class-aware mapping + `raw_sha` + idempotency keys; **no Supabase client / no SQL·RPC / no live MT5 / no network** (tests PASS incl. import-scan, deterministic output). It is inert tooling — **not** the writer. Real staging writer stays gated behind reviewed schema/RLS + explicit DB-write approval + role decision + Supabase write tests + rollback plan.
- **E — GUGU bot.** Cognition/autonomous behavior is frozen; capture-only. Any cadence/cognition change = STOP.
- **F — Merge vs grouping. AUDITED (`8c01f95`, verdict CLEAR_FOR_BATCH_DEPLOY_AS_IS).** Legacy Merge disabled/unreachable (sole `🔗` entry `disabled`, no onClick); no merge path writes `group_id`; durable paths preserve `group_id` by omission; no confusing labels. The one data-safe gap (grouping candidates could include `isMerged` rows) is FIXED at the UI/candidate layer (`b1f8e7d`). The defense-in-depth RPC-side `raw->>'isMerged'` reject is **designed + reviewed (ChatGPT PASS, APPROVED)** ([`../g2_grouping/g2_rpc_ismerged_hardening_design.md`](../g2_grouping/g2_rpc_ismerged_hardening_design.md)): add one `merged_child_not_allowed` error to `create_trade_group_v1` (unconditional §3 check via crash-safe `lower(btrim(coalesce(raw->>'isMerged','')))='true'`), function-body-only replace, ungroup unchanged. **Sequenced AFTER_V04_DEPLOY, before write-gate enable**; SQL apply is user-run in the SQL Editor with BEGIN/ROLLBACK tests. **Mandatory pre-apply precheck** (read-only): confirm 0 active grouped children with `raw->>'isMerged'` truthy (expected 0 given 0 active groups) — if >0, STOP and review before applying.
- **G — RLS/security.** Requires a fresh read-only audit; never edit RLS/policies under autopilot.
- **H — P2-5 image externalization.** Closed/backfilled. Sensitive raw backup lives outside git; delete only on explicit approval.
- **I — Mentor/GUGU notes.** Backlog only.
